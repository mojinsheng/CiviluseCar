<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.xslong.xslonglib"
    >

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".picture.squarecamera.RuntimePermissionActivity"
            android:theme="@style/squarecamera__CameraFullScreenTheme.NoUI">
        </activity>
        <activity
            android:name=".picture.squarecamera.CameraActivity"
            android:theme="@style/squarecamera__CameraFullScreenTheme"/>
        <activity android:name=".imagePager.BigImagePagerActivity"/>
    </application>

</manifest>
